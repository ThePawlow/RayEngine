using RayEngine.SceneControl;

namespace RayEngine.Examples;

public class RacingGame: Scene2D
{

        public RacingGame(string name, bool isActive = false) : base(name, isActive)
        {
        }
}